

#include<iostream>


using namespace std;

int main()
{
  double average = 0;
  double number = 0;
  double grade = 0;
  double total = 0;
  int size;

  
  while (true )
  {
  cout<<"Input grades: "<<endl;
  cin>>grade;
  if(grade == -1)
  {
    break;
  }
  size++;
  total+=grade;
  }
  average = total / size;
  cout<<"The average is "<<average<<endl;
  return 0;

}
